@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated) {
    using (Html.BeginForm("LogOff", "Account", new { area=""}, FormMethod.Post, new { id = "logoutForm"})) {
        @Html.AntiForgeryToken()


            <li>
                <a href="javascript:document.getElementById('logoutForm').submit()"> <i class="fa fa-sign-out fa-fw"></i>Cerrar Sesi&oacute;n!!!</a>
            </li>
    }
}
else
{
  
        <li>
            <a href="@Url.Action("Login","Account",new { area = "" })"><i class="fa fa-sign-in fa-fw"></i>Iniciar Sesi&oacute;n!!!</a>
        </li>
  
    
    
}
